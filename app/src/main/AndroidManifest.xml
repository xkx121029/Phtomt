<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Termux 执行通道：该权限由 Termux 声明（dangerous 级），授予后本 App 才能向
         com.termux.app.RunCommandService 下发命令；未安装 Termux 时该权限无法授予，不影响安装 -->
    <uses-permission android:name="com.termux.permission.RUN_COMMAND" />

    <!-- 包可见性：Termux 执行通道需要探测 com.termux 是否安装 -->
    <queries>
        <package android:name="com.termux" />
    </queries>

    <application
        android:name=".PhoneAgentApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhoneAgent"
        tools:targetApi="34">

        <!-- windowSoftInputMode 必须显式声明为 adjustResize：
             不声明时系统按 adjustUnspecified 自行选择，非滚动容器（Compose 根视图不是 ScrollView）
             可能被判成 adjustPan —— 系统把整个窗口内容往上平移，而输入区自己又用 imePadding()
             避让 IME，两者叠加就会把输入区顶到比键盘顶更高，中间空出一块。
             edge-to-edge（enableEdgeToEdge + targetSdk 35）下 adjustResize 不会压缩窗口，
             IME 只通过 WindowInsets.ime 上报，由页面内的 imePadding() 决定输入区位置。 -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PhoneAgent"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".device.a11y.AgentAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="@string/a11y_service_label">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".device.screen.ScreenSharingService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <service
            android:name=".overlay.FloatingWindowService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="com.phoneagent.agent_progress_overlay" />
        </service>

        <service
            android:name=".feature.edge.EdgeLightingService"
            android:exported="false" />

        <!-- 点击光标覆盖层：任务执行时显示圆润指针，指示 AI 正在点哪里 -->
        <service
            android:name=".overlay.CursorOverlayService"
            android:exported="false" />

        <!-- 无线 ADB 配对：通知栏内联输入配对码的回调接收器 -->
        <receiver
            android:name=".device.shell.AdbPairingReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.phoneagent.action.RECEIVE_PAIRING_CODE" />
            </intent-filter>
        </receiver>

        <!-- Termux 执行通道：接收 Termux 通过 PendingIntent 回传的命令结果（自收自发，无需 exported） -->
        <receiver
            android:name=".device.shell.TermuxResultReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.phoneagent.action.TERMUX_RESULT" />
            </intent-filter>
        </receiver>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>

</manifest>